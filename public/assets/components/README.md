# Component Visual Assets

The catalog points at transparent PNG image assets in this folder. Editable SVG placeholders are kept beside them for quick iteration, and the PNGs can be replaced with production renders using the same dimensions:

- `width = moduleWidth * 18px`
- `height = 120px`

The application renders these assets as images and scales placement using the module-based board constants.
